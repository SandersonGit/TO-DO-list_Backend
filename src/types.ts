export type TusersDB = {
    id: string,
    name: string,
    email: string,
    password: string
}

export type TtaskDB = {
    id: string,
    title: string,
    description: string,
    created_at: string,
    status: number 
}
